using System;

namespace Features_CSharp
{
    class Program
    {
        static void Main(string[] args)
        {
            /* 1. Smart Tags (Ctrl + .)
             * 2. Fore IntelliSense to show (Ctrl + Space)
             * 3. Search in code (Ctrl + I)
             * 4. Quick info show (Ctrl + K, I)
             * 5. Cut code line (Ctrl + L)
             * 6. Move cursor to above line (Ctrl + Enter)
             * 7. Move selected code above/doewn (Alt + Up/Down)
             * 8. Select Code Vertically (Shift + Alt + Up/Down)
             * 9. Auto format code (Ctrl + K, D)
             * 10. Comments selected code (Ctrl + K, S)
             * 11. #region (#region + Tab, Tab)
             * 12. Surround with region (Ctrl + K, S -> select #region) 
             * 13. Go to end bracket (Click on opening bracket -> Ctrl + ])
             * 14. Navigate to any file (Ctrl + ,)
             * 15. Debug the application (F5)
             * 16. Run to cursor (Ctrl + f10) */
        }
    }
}